import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // ใช้ App.jsx ได้เลย ไม่ต้องเปลี่ยนชื่อ

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
